# app log

`app.log` is a captured excerpt of production traffic.

The on-call runbook says: when triaging an incident, first pull the ERROR lines out
into `errors.txt` so they can be attached to the incident ticket.
